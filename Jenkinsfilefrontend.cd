pipeline {
    agent any


    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies & Build') {
            steps {
                sh '''
                cd ansible
                ansible-playbook -i inventory.ini playbook.yml
                '''
            }
        }       
    }

    
}